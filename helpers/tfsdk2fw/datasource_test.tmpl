// Code generated by helpers/tfsdk2fw. Manual editing is required.
// This file only implements migration test. Copy existing tests here.

package {{ .PackageName }}_test

import (
    "testing"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
	"github.com/vk-cs/terraform-provider-vkcs/vkcs/internal/acctest"
)

func TestAcc{{ .PackageName | upper_first }}{{ .Name }}DataSource_migrateToFramework(t *testing.T) {
	resource.Test(t, resource.TestCase{
		PreCheck: func() { acctest.AccTestPreCheck(t) },
		Steps: []resource.TestStep{
			{
				ExternalProviders: map[string]resource.ExternalProvider{
					"vkcs": {
						VersionConstraint: "{{ .ReleasedVersion }}",
						Source:            "vk-cs/vkcs",
					},
				},
				Config: testAcc{{ .PackageName | upper_first }}{{ .Name }}DataSourceBasic,
				// Check: add check from basic test
			},
			{
				ProtoV6ProviderFactories: acctest.AccTestProtoV6ProviderFactories,
				Config:                   testAcc{{ .PackageName | upper_first }}{{ .Name }}DataSourceBasic,
				PlanOnly:                 true,
			},
		},
	})
}

// Use basic config for migration test.
const testAcc{{ .PackageName | upper_first }}{{ .Name }}DataSourceBasic = `
`
